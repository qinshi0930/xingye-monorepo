# Redis 基础配置
# 数据持久化
appendonly yes
appendfsync everysec

# 内存管理
maxmemory 256mb
maxmemory-policy allkeys-lru

# 安全配置
# 注意：密码通过 podman-compose.infra.yml 的 command 参数传递

# 日志配置
loglevel notice

# 客户端连接
timeout 0
tcp-keepalive 300
